apiVersion: v1
kind: Service
metadata:
  name: {{ include "moogla.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "moogla.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 11434
